import 'package:flutter/material.dart';

const WITH1 = Color(0xFFF1EFF1);
const ORANG1 = Color(0xFFFFA41B);
const SHIN = Color(0xFF035AA6);
const MOR = Color(0xFF000839);
const RASASI = Color(0xFF747474);
const AWE = Color(0xFF40BAD5);
const kasko = Color.fromARGB(230, 234, 194, 184);
const rash = Color.fromARGB(115, 34, 33, 33);

/*const shino = LinearGradient(colors: [
  Color.fromARGB(220, 7, 72, 48),
  Color.fromARGB(210, 1, 25, 10),
]);*/

const PADING = 10.0;

//OUR SHADO
const KBSHADOW = BoxShadow(
  offset: Offset(0, 15),
  blurRadius: 27,
  color: Colors.black12,
);
